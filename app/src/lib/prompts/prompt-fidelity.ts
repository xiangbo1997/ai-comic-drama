/**
 * 提示词忠实性铁律（单一真源）
 *
 * 规则来源：Toonflow storyboard_prompt_techniques「分镜表内容忠实性原则」蒸馏——
 * 「提示词生成是格式转换，不是创意写作」。分镜/场景描述是提示词的唯一内容来源，
 * LLM 在把它改写/扩写成图像或视频提示词时，只做表达形式转换，不做创意发散。
 *
 * 为什么独立成文件（而非并入 adaptation-rules.ts）：
 * - adaptation-rules 治的是「小说→剧本」的外化工序（内心戏 → 可见动作），是**改编**创作，
 *   本就允许并要求创造性改写（把心理词换成镜头语言）。
 * - 本铁律治的是「分镜/场景 → 图像/视频提示词」的**格式转换**工序，恰恰**禁止**创作发散，
 *   要求逐字段零遗漏地保留画面元素。二者语义相反，混在一起会互相冲突，故拆开。
 *
 * 只注入「由 LLM 改写/扩写画面或视频提示词」的环节（storyboard / storyboard-table /
 * video-director）；确定性拼接器（buildEnhancedPrompt / script.ts 的
 * generateImagePrompt）不注入——它们只组装不创作；prompt-suggest 是创意建议
 * （ideation）环节，与本铁律目的相反，同样不注入。
 */

/** 提示词忠实性铁律：注入 LLM 写画面/视频提示词的 system prompt。 */
export const PROMPT_FIDELITY_RULES = `【提示词忠实性铁律（最高优先级）】
- 提示词生成是「格式转换」不是「创意写作」：分镜/场景描述是提示词的**唯一内容来源**。
- 画面描述的所有视觉元素（主体 / 物件 / 空间关系 / 动态 / 镜头关系）必须完整保留，零遗漏。
- 语义等价转换：只改表达形式（中↔英、叙事↔关键词），不改语义；禁止创意发散——不添加原描述没有的装饰元素（如原文没写花瓣飘落就不加），不重新诠释情绪氛围（原文「冷傲」不可改成「忧伤」）。
- 风格词从属于内容：风格 / 画质 / 光影修饰词服务于画面描述，与具体画面冲突时以画面描述为准；token 紧张时宁可缩风格词，不可删画面元素。
- 输出前逐字段回溯校验：画面主体 / 景别 / 动作 / 情绪 / 光影是否与来源字段一一对应，不通过则修正再输出。`;
